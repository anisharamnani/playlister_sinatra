<ul> 
	<h2> <%= @artist_name %> </h2>
		<% 
			searched = Artist::ARTISTS.detect{|artist| artist.name.upcase == @artist_name.upcase}
			searched.songs.each do |song|
		%>
		<li>
		<a href=<%="/songs/#{song.name}"%>>	
			<%= 
				"#{song.name}" 
			%>
		</a>
		</li>
		<%end%>
</ul> 